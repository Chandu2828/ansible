- name: install packages
  hosts: node
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - git
    - mongodb-mongosh