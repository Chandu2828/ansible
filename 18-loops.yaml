- name: install packages
  hosts: node
  become: yes
  tasks:
  - name: install packages
    package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "nginx", state: "Present"}
    - { name: "mysql", state: "absent"}
    - { name: "zip", state: "present"}
    